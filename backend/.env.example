# Copie este arquivo para .env e preencha os valores
# cp .env.example .env

# Django
SECRET_KEY=django-insecure-TROQUE-ESTA-CHAVE-EM-PRODUCAO
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
# Em produção: ALLOWED_HOSTS=20.220.25.219,aerorioclaro.duckdns.org
CORS_ALLOWED_ORIGINS=http://localhost:3000
# Em produção: CORS_ALLOWED_ORIGINS=http://aerorioclaro.duckdns.org

# Banco de dados (MariaDB)
DB_NAME=aeroclube
DB_USER=aeroclube
DB_PASSWORD=aeroclube123
DB_ROOT_PASSWORD=root123
DB_HOST=db
DB_PORT=3306

# Banco de testes
DB_TEST_NAME=aeroclube_test

# E-mail (recuperação de senha)
# Para desenvolvimento: deixe EMAIL_BACKEND como console para ver os e-mails no log
# Para produção com Gmail: gere uma Senha de App em myaccount.google.com/apppasswords
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=projetointegrador.ifsp2026@gmail.com
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=Aeroclube <projetointegrador.ifsp2026@gmail.com>
FRONTEND_URL=http://localhost:3000
